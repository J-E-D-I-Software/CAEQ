extends baseEmail

block content
  p Hola Arq. #{firstName},
  p Se confirmó su pago y ha sido inscrito al curso:
  p #{courseName}.
  p Modalidad: #{courseModality}.
  p El curso inicia #{courseStartDate} y termina #{courseEndDate}.
  p Horario del curso: #{courseSchedule}.
  p Horas que acreditará con el curso: #{courseHours} horas.
  table.btn.btn-primary(role='presentation', border='0', cellpadding='0', cellspacing='0')
    tbody
      tr
        td(align='left')
          table(role='presentation', border='0', cellpadding='0', cellspacing='0')
            tbody
              tr
                td
                  img(src=`${courseImageUrl}`, style='width:100%')